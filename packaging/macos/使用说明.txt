歌词转换工具 - macOS 启动入口

使用方法：

1. 双击：
   启动歌词转换工具.command

2. 第一次启动会自动创建 .venv 并安装依赖。
   这个过程可能需要几分钟。

3. 稍等几秒，浏览器会自动打开：
   http://localhost:8501

4. 使用期间不要关闭终端窗口。
   如果关闭窗口，工具会停止运行。

注意：

- Mac 需要已经安装 python3。
- 如果双击提示没有权限，请在终端中运行：
  chmod +x packaging/macos/启动歌词转换工具.command
- 歌词内容只在本机运行，不会上传到外部服务器。
